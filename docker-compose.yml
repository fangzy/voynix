services:
  voynix-xray:
    build:
      context: ./docker-image
      dockerfile: Dockerfile
      args:
        APK_MIRROR: mirrors.aliyun.com
    ports:
      - "8089:8089"
    environment:
      - UUID=${UUID}
      - WS_PATH=${WS_PATH:-ws}
    restart: unless-stopped
