UUID=your-uuid-here
# 32-128 字符,Base64 字符集(openssl rand -base64 48)
FC_BEARER_TOKEN=your-bearer-token-here
# ws(默认) | grpc(回退)
TRANSPORT=ws
# WS path
WS_PATH=ws
# gRPC 回退传输用
GRPC_SERVICE_NAME=ProxyService
